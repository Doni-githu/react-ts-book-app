export interface IStyles {
    paddingX: string,
    paddingY: string,
    padding: string,
    heroHeadText: string,
    heroSubText: string,
    sectionHeadText: string,
    sectionSubText: string
}
